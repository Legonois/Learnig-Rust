fn main() {
    println!("Hello, world!")
    // the ! in println! indicates that it is a macro, not a function. 
    // Macros are a way to write code that writes other code, which is why they are used in Rust for metaprogramming.
}