
fn main() {
    println!("Hello, world!")

    // Q: What does the ! mean in the println! macro?
    // A: The ! means that println! is a macro, not a function. Macros are a way to write code that writes other code, which is why they are used in Rust for metaprogramming.
}